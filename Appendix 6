void setup(){ 
    pinMode(R_Sensor, INPUT); 
    pinMode(L_Sensor, INPUT); 

    pinMode(EN_A, OUTPUT); 
    pinMode(EN_B, OUTPUT);

    pinMode(IN1, OUTPUT); 
    pinMode(IN2, OUTPUT); 
    
    digitalWrite(EN_A, HIGH); 
    digitalWrite(EN_B, HIGH); 

    delay(1000);
}
